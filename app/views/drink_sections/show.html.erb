<div class = "menubody">

    <a><h2><%= @drink.name %></h2></a>

    <ul>
      <% @drink_items.each do |drink_item| %>
        <li>
          <span class="item"><%=drink_item.name%></span>, <span class="description"><%=drink_item.description%></span>
          <span class="region"><%=drink_item.region%></span> <span class="pricing">(<%=drink_item.pricing%>)</span>
        </li>

        <div class="buttons">
          <li>
            <form action="/drink-items/edit/<%=drink_item.id %>" method="POST">
              <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>" />
              <input type="hidden" name="_method" value="GET" />
              <input type="submit" value="Edit Drink Item" />
            </form>
          </li>
          <li>
            <form action="/drink-items/<%=drink_item.id %>" method="POST">
              <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>" />
              <input type="hidden" name="_method" value="DELETE" />
              <input type="submit" value="Delete Drink Item" data-confirm="Delete the drink item?" />
            </form>
          </li>
        </div>
      <% end %>
    </ul>

    <h4>20% Service Charge**</h4>
    <p class ="notes">
      Please let us know if you are allergic to anything.<br>
      ** 100% of this charge is paid to employees. 55% of this charge is paid to employees “directly serving the customer”.
    </p>
    <h4>Thank you for dining at the Walrus and the Carpenter.</h4>

</div>


    <% if @drink_item %>
      <ul class= "popup">
        <h3>ERROR!</h3>
        <% @drink_item.errors.each do |column, error| %>
          <li><%= column %> <%= error %></li>
        <% end %>
        <p class = "errors">X close error messages X</p>
      </ul>
    <% end %>


  <div class = "box">

    <form action="/drinks/<%=@drink.id%>" method="POST">
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>" />
      <div>
        <h4>
          <label for="name-input">Create A Drink Item</label>
        </h4>
        <input type="text" id = "name-input" name="drink_item_name" />
      </div>
      <div>
        <h4>
          <label for="description-input">Description</label>
        </h4>
        <input type="text" id = "description-input" name="drink_item_description" />
      </div>
      <div>
        <h4>
          <label for="region-input">Region</label>
        </h4>
        <input type="text" id = "region-input" name="drink_item_region" />
      </div>
      <div>
        <h4>
          <label for="pricing-input">Pricing</label>
        </h4>
        <input type="string" id = "pricing-input" name="drink_item_pricing" />
      </div>
      <div class="submit">
        <input type="submit" />
      </div>
    </form>
  </div>

  <div class = "home">
    <a><%= link_to "Food", sections_path %></a>
    <a><%= link_to "Drinks", drinks_path %></a>
  </div>
