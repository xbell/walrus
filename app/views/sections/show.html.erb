
<div class = "menubody">

    <h2><%= @section.name %></h2>

    <ul>
      <% @items.each do |item| %>
        <li>
          <span class= "item"><%=item.name%></span>, <span class = "description"><%=item.description%></span> <span class = "price">(<%=item.price%>)</span> <%=item.note%>
        </li>
        <%= link_to "Edit Item", edit_item_path(item) %>
        <%= link_to "Delete Item", delete_item_path(item) %>
      <% end %>
    </ul>

    <h4>20% Service Charge**</h4>
    <p class="notes">
      *Consuming raw, undercooked, or unpasteurized foods may increase your risk of foodborne illness. <br>
      Menu items may contain nuts and other allergens.
      Please let us know if you are allergic to anything.
    </p>
    <p class ="notes">
      ** 100% of this charge is paid to employees. 55% of this charge is paid to employees “directly serving the customer”.
    </p>
    <h4>Thank you for dining at the Walrus and the Carpenter.</h4>

</div>


    <% if @item %>
      <ul class= "popup">
        <h3>ERROR!</h3>
        <% @item.errors.each do |column, error| %>
          <li><%= column %> <%= error %></li>
        <% end %>
        <p class = "errors">X close error messages X</p>
      </ul>
    <% end %>


  <div class = "box">

    <form action="/sections/<%=@section.id%>" method="POST">
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>" />
      <div>
        <h4>
          <label for="name-input">Create A Menu Item</label>
        </h4>
        <input type="text" id = "name-input" name="item_name" />
      </div>
      <div>
        <h4>
          <label for="description-input">Item Description</label>
        </h4>
        <input type="text" id = "description-input" name="item_description" />
      </div>
      <div>
        <h4>
          <label for="description-input">Price</label>
        </h4>
        <input type="text" id = "description-input" name="item_price" />
      </div>
      <div>
        <h4>
          <label for="description-input">Note</label>
        </h4>
        <input type="text" id = "description-input" name="item_note" />
      </div>
      <div>
        <input type="submit" />
      </div>
    </form>
  </div>

  <div class = "home">
    <%= link_to "Home", sections_path %>
  </div>
